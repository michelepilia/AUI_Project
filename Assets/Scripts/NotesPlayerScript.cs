using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NotesPlayerScript : MonoBehaviour
{

    public GameObject playerCamera;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

   

   /* void OnMouseDown() {
        RaycastHit hit;
        Ray downRay = new Ray(playerCamera.GetComponent<Transform>().position, Vector3.down);
        if(Physics.Raycast(downRay, out hit)){
            Debug.Log("Prova");
        }
    }
    */
}
